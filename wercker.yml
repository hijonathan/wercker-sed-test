box: wercker/nodejs

build:
    steps:
        - script:
            name: Yo
            code: echo sup world

deploy:
    steps:
        - script:
            name: Env.js
            code: ENV_JSON=$ENV_JSON node create-env.js

        - hijonathan/gh-pages:
            token: $GIT_TOKEN
