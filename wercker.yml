box: wercker/nodejs

build:
    steps:
        - script:
            name: Yo
            code: echo sup world

deploy:
    steps:
        - script:
            name: Env.js
            code: |
                node create-env.js

        - hijonathan/gh-pages:
            token: $GIT_TOKEN
