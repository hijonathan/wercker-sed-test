box: wercker/nodejs

build:
    steps:
        - script:
            name: Yo
            code: echo sup world

deploy:
    steps:
        - script:
            name: Env.js
            code: |
                export OUTPUT_DIR="$WERCKER_SOURCE_DIR/__"
                export ENV_JSON=$ENV_JSON
                node create-env.js

        - hijonathan/gh-pages:
            token: $GIT_TOKEN
