box: wercker/nodejs

build:
    steps:
        - script:
            name: Yo
            code: echo sup world

deploy:
    steps:
        - script:
            name: Env.js
            code: |
                ls $WERCKER_SOURCE_DIR
                mkdir -p $WERCKER_SOURCE_DIR/__/
                echo "(function() {window.__env = " $ENV_JSON "})();" > $WERCKER_SOURCE_DIR/__/env.js

        - hijonathan/gh-pages:
            token: $GIT_TOKEN
